spring.application.name=StockWatch Pro
server.port=8080
server.servlet.context-path=/api

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/stockmanagement
spring.datasource.username=postgres
spring.datasource.password=21184114
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Liquibase Configuration
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml

# JWT Configuration
jwt.signing.key=ZU&c0z%vNIYkCUWFK(U+6.KD%kM91q}ztHgX%PppbX,uT~$C.ZU&c0z%vNIYkCUWFK(U+6.KD%kM91q}ztHgX%PppbX,uT~$C.ZU&c0z%vNIYkCUWFK(U+6.KD%kM91q}ztHgX%PppbX,uT~$C.
jwt.issuer=http://localhost:8080
jwt.audience=http://localhost:8080
jwt.expiration=30

# Logging
logging.level.root=INFO
logging.level.com.stockwatch.stockwatchpro=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.expose-headers=Authorization
cors.allow-credentials=true

# Jackson Configuration
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.indent-output=true

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.deep-linking=true
springdoc.swagger-ui.urls.primary.name=StockWatch Pro API
